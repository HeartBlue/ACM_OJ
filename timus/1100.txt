#include <cstdlib>  
#include <cctype>  
#include <cstring>  
#include <cstdio>  
#include <cmath>  
#include <algorithm>  
#include <vector>  
#include <string>  
#include <iostream>  
#include <sstream>  
#include <map>  
#include <set>  
#include <queue>  
#include <stack>  
#include <fstream>  
#include <numeric>  
#include <iomanip>  
#include <bitset>  
#include <list>  
#include <stdexcept>  
#include <functional>  
#include <utility>  
#include <ctime>
using namespace std;
typedef long long LL;
typedef unsigned long long ULL;
#define MAX(a,b) ((a) > (b) ? (a) : (b))
#define MIN(a,b) ((a) < (b) ? (a) : (b))
#define MEM(a,b) memset((a),(b),sizeof(a))
const LL INF = 1000000007;
const int N = 1e5;
int a[N];
class Player
{
public:
	int id;
	int s;
	bool operator < (const Player &p) const
	{
		return s > p.s;
	}
};

vector<Player> vp;
int main()
{
	//freopen("input.txt", "r", stdin);
	int n;
	cin >> n;
	vp.resize(n);
	for(int i = 0; i < n; i++)
	{
		cin >> vp[i].id >> vp[i].s;
	}
	stable_sort(vp.begin(),vp.end());
	for(int i = 0; i < n; i++) cout << vp[i].id << ' ' << vp[i].s << endl;
	return 0;
}