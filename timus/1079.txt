#include <cstdlib>  
#include <cctype>  
#include <cstring>  
#include <cstdio>  
#include <cmath>  
#include <algorithm>  
#include <vector>  
#include <string>  
#include <iostream>  
#include <sstream>  
#include <map>  
#include <set>  
#include <queue>  
#include <stack>  
#include <fstream>  
#include <numeric>  
#include <iomanip>  
#include <bitset>  
#include <list>  
#include <stdexcept>  
#include <functional>  
#include <utility>  
#include <ctime>
using namespace std;
typedef long long LL;
typedef unsigned long long ULL;
#define MAX(a,b) ((a) > (b) ? (a) : (b))
#define MIN(a,b) ((a) < (b) ? (a) : (b))
#define MEM(a,b) memset((a),(b),sizeof(a))
const LL INF = 1000000007;
const int N = 1e5;
int a[N];
int main()
{
	//freopen("input.txt", "r", stdin);
	a[0] = 0;
	a[1] = 1;
	for(int i = 2; i < N; i++)
	{
		if(i&1) a[i] = a[i>>1] + a[i-(i>>1)];
		else a[i] = a[i>>1];
	}
	for(int i = 2; i < N; i++) a[i] = max(a[i],a[i-1]);
	int n;
	while(cin >> n)
	{
		if(n == 0) break;
		cout << a[n] << endl;
	}
	return 0;
}